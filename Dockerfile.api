FROM python:3.12-slim

WORKDIR /app

# 기본 패키지 복사 및 의존성 주입
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 코드 전체 복사 (Dockerfile 내부)
COPY . .

# FastAPI 서버 구동
EXPOSE 8000
CMD ["uvicorn", "api.main:app", "--host", "0.0.0.0", "--port", "8000"]
